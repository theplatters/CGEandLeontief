# src/closures/registry.jl — id → constructor mapping (relocation placeholder)
#
# Phase 2, Part 1: placeholder so the include order is fixed. The full
# registry (closure_ids / closure_axis / closure_constructor plus the
# BF/ALPHA/BETA/GAMMA builders) lands in Part 2/3. Status lives only in
# registry/closures.toml (ADR-0003); this file carries no status.
