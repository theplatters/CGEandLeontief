# Lab log — September 2026

Append-only chronological record: one entry per working session or experiment
batch. This is the narrative history; status lives in `registry/` and
`docs/status.md` (ADR-0003), decisions in `docs/decisions/`, abandoned
approaches in `docs/dead-ends/`. Log entries reference those records instead
of restating them.

Format: **intent → what ran (run ids/commits) → observation → decision →
next.** Failed batches are logged too; do not delete entries.

## 2026-09-17 — Phase 0: tracking backbone (branch `reorg`, freeze commit `dfd60a2`)

**Intent.** End the "which document is right?" problem: four model kernels,
contradictory status documents (`selective_status_overview.md` claims
mobile-labour "GO" and "88.4%" while `roadmaps/vertdict.md` and `ROADMAP.md`
declare those results invalid), dead ends recorded only in prose, and no
traceable link from claims to code, data, and runs.

**Done (Phase 0).**

- `registry/` created as the single source of truth: `closures.toml`
  (6 labour closures BF/ALPHA/BETA/GAMMA/DELTA/ZETA, 3 financing F1–F3,
  nothing `validated`), `scenarios.csv` (21 rows: the planned 5×3 matrix plus
  six recorded `cbase2-v3` verification runs, three of them `failed` and
  three `provisional`), `freeze.toml` (3 frozen snapshots + 3 source archives
  + 3 legacy notebooks, with commits and git tree hashes).
- `scripts/status.jl` generates `docs/status.md` from the registry; `--check`
  fails on staleness.
- Decision records ADR-0001 (one kernel, no living copies), ADR-0002
  (closure taxonomy and stable ids), ADR-0003 (registry is the single source
  of truth), ADR-0004 (runs are immutable manifests).
- Dead-end register seeded with DE-0001 … DE-0008 (unnormalized shifter;
  dropped zero-profit equation; `η=10⁶` as fixed wage; nominal-wage supply;
  fixed-base GDP sum; renormalized variance shares; BF 2022 as base model;
  cbase2 v2 open economy without a saving rate).
- Legacy zones marked frozen/read-only (`cbase2/FROZEN.md`,
  `bf_replication/FROZEN.md`, `bf_replication2/FROZEN.md`, freeze records).
- `selective_status_overview.md` and `docs/archive/` marked superseded.

**Freeze.** `cbase2` is frozen at `dfd60a2` (tree hash
`a8241e98ef61e572910c3a43d49de2fecc32b9e2`, 45 files; two recorded kernel
DIFFs). Remaining pipeline work — BETA/DELTA promotion, the 5×3 matrix, the
v3 open items (Törnqvist base, F3 stall, canary/omitted N-th market,
θ-bounded-at-1 continuation stall, sector-71 instability) — moves to the
root package in Phase 2. `cbase2/review.md` findings are recorded in
`registry/freeze.toml` `open_items` and closure `open_gates`; none is closed
by this reorganization.

**Observation (recorded, not resolved).** `cbase2/documentation.md` lists
notebooks 04–08, `src/validation.jl`, and `scripts/run_*.jl` that do not
exist; `bf_replication` sources disagree on the oil-shock headline; the
`bf_replication2` README and `VALIDATION.md` disagree on HtM convergence.
These contradictions are now visible in the freeze record.

**Decisions.** ADR-0001 … ADR-0004. No model or results claims were changed
by Phase 0.

**Dead ends.** DE-0001 … DE-0008 (seed).

**Next.**

1. Phase 1 — untrack generated artifacts (`.ipynb_checkpoints/`,
   generated plots, logs), archive the six orphan `src/*.jl` files
   (`ces_temporal.jl`, `goodwin.jl`, `loaders.jl`, `main.jl`, `mvnrnd.jl`,
   `mobile_labor-JK.jl`), resolve the heavy-dependency split.
2. Phase 2 — promote BETA/DELTA/F1–F3 from the frozen `cbase2` into
   `src/closures/` behind the registry; resolve the freeze `open_items` in
   the canonical kernel.
3. Phase 3 — `experiments/run.jl` + manifests + `scripts/check_repo.jl`,
   rewrite `AGENTS.md` as the operating contract, port the preregistration
   gate.

**Pre-history.** `cbase2/process_comments.md` (dated entries 2026-09-15 to
2026-09-17) and `cbase2/review.md` are the detailed record of the cbase2
work and remain the evidence behind the registry entries.

## 2026-09-17 — Phase 1: cleanup and agent operating contract (branch `reorg`)

**Intent.** Stop the bleeding after Phase 0: `src/` still held six files that
were never wired into the module, generated artifacts were tracked, the root
environment carried the full heavy stack, and the new tracking system had no
agent-facing skills. (Reorg phases are counted separately from the model
gates in `ROADMAP.md`: reorg 0 = tracking backbone, 1 = cleanup,
2 = promotion from cbase2, 3 = experiments/manifests/checker.)

**What ran (commits on `reorg`).**

- `deadend(0009)` `2b12403` — moved `ces_temporal.jl`, `goodwin.jl`,
  `loaders.jl`, `main.jl`, `mvnrnd.jl` and `mobile_labor-JK.jl` to
  `archive/src-orphans/` (read-only); DE-0009 records the classification.
  The two frozen legacy notebooks that `include("src/mvnrnd.jl")` were not
  edited and must be repointed to the archive path if ever rerun.
- `0128eb7` — untracked 4 `.ipynb_checkpoints/` files, 13 `plots/*.png`,
  `rerun_results.log` and `Manifest.toml` (all kept on disk; `/plots/*.png`
  now ignored). `prices.csv` and `sector_names.txt` moved to `data/`; no live
  references existed, and the untracked `data/sector_names.txt` copy was
  byte-identical.
- `5047b26` — plotting moved into the GLMakie package extension
  (`ext/BeyondHultenGLMakieExt.jl`; stubs in `src/plots.jl` raise an
  instructive error). `Project.toml` `[deps]` cut 35 -> 12; removed the
  heavy/unused stack (Ipopt, JuMP, MathOptInterface, Plots, MAT, XLSX,
  ModelingToolkit, OrdinaryDiffEq, StaticArrays, Measures, Tables,
  LineSearches, StatsBase, DelimitedFiles and nine zero-reference packages).
  `using BeyondHulten` no longer loads GLMakie and `Pkg.test()` passes.
- `0341b1a` — added `.opencode/skills/{tracking,closures,experiments}` and
  rewrote `AGENTS.md` as the operating contract (registry, freeze, evidence
  gates, ADR/DE/log, commit conventions); the style guidance was dropped and
  the AGENTS.md rewrite was pulled forward from the Phase 3 list.
- `5f0f884` — registered `archive/` as read-only with Phase 1 baseline commit
  `2b12403` (tree hash `80b32322…`), since the zone did not exist at the
  Phase 0 freeze commit.
- `adr(0002)` `91fb125` — amendment: DELTA is the IO-endpoint row of the 5x3
  matrix reached as GAMMA + Leontief technology, not an independent
  mechanism; the Phase 0 wording ("never an independent matrix row") was
  wrong.

**Consequences recorded.** Rerunning the frozen `cbase2` pipeline or the
legacy notebooks now requires re-adding their old dependencies (`XLSX`, `MAT`,
`JuMP`, `Ipopt`, ...) in a scratch environment; plotting requires
`Pkg.add("GLMakie")`. `Manifest.toml` is untracked, so the ignore rule of
`31c5a82` finally takes effect.

**Observation.** The Phase 0 registries and skills paid off immediately: the
skill review flagged the DELTA contradiction, the missing `archive/` freeze
record, and the tracked-despite-ignored `Manifest.toml`; all three were fixed
before this entry.

**Next.** Phase 2: promote BETA, DELTA and F1–F3 from the frozen `cbase2`
into the root kernel (target `src/closures/`), resolving the cbase2 open items
(Törnqvist base, F3 stall, omitted N-th market, theta continuation) there.

## 2026-09-17 — Phase 2: kernel relocation and cbase2 promotion (branch `reorg`)

**Intent.** Relocate the canonical kernel into `src/core/` + `src/closures/`
with no logic change, then promote BETA, DELTA and F1–F3 from the frozen
`cbase2` snapshot into the root package, resolving the recorded kernel DIFFs
and the Phase 1 open items — behind the hard gate that
`tests/test_kernel_regression.jl` (commit `8dd9907`) stays green and untouched.

**What ran (commits on `reorg`).**

- `40b2bb0` — relocation only: `src/interface.jl` → `src/core/accounting.jl`
  (closure types moved to `src/closures/labor/types.jl`), `src/ces.jl` +
  `src/leontief.jl` + `src/cobbdouglas.jl` → `src/core/technology.jl`,
  `src/solution.jl` + `src/mobile_labor.jl` → `src/core/equilibrium.jl`,
  `src/util.jl` + `src/impulses.jl` + `src/variance_decomposition.jl` →
  `src/core/diagnostics.jl`; stubs for `src/closures/labor/labor.jl`,
  `src/closures/financing/financing.jl`, `src/closures/registry.jl`;
  `src/BeyondHulten.jl` include order fixed;
  `rerun_results.jl` include paths updated. Full suite green.
- `580327d` — kernel backport + promotion: `Data` gains the six v3
  absorption fields (cbase2 names/order) with legacy compatibility defaults,
  `Model` gains `financing` (3-arg defaults to `NoFinancing()`),
  `eta_s`/`:beta` + `labor_market_residual` hook, F1–F3 hooks, financed
  demand PLUS retained manna A/G, CD-limit guards, residual-gated LM polish
  (mobile 1e-5, fixed 1e-6), all-N fixed formulation (`w = 1` numeraire),
  flexible `equilibrium_residuals` lengths, `household_baseline` Törnqvist
  base with NaN guard, `financing`/`eta_s` kwargs; BETA/DELTA economics into
  `src/closures/labor/labor.jl`; `src/closures/registry.jl` id→constructor
  mapping (`ZETA` → `nothing`); new exports. Full suite green, including the
  characterization goldens.
- Registry/docs/ADR commit — `registry/closures.toml` files/symbols/
  references/open_gates updated (promotion gates removed, contract-tests
  gates kept, status stays `implemented`); `registry/freeze.toml`
  `[frozen.cbase2]` backport note (hashes/commits unchanged);
  `docs/closures/{beta,delta,financing}.md` (cbase2 design notes preserved);
  `docs/decisions/ADR-0005-phase2-layout-and-backport.md` + README index;
  living path references fixed (skills, `AGENTS.md`, `docs/definitive_guide.md`,
  `registry/README.md`); frozen/historical docs (`cbase2/`, notebooks,
  `roadmaps/vertdict.md`, `docs/archive/`, dead ends) untouched.
- Log commit — this entry.
- Status commit — regenerated `docs/status.md` (`scripts/status.jl`:
  0 warnings; `--check` clean).

**Observation.**

- Mobile characterization goldens unchanged (η = 0/0.5/1, additive-shock
  variant, tiny fixture, contracts) — the financed+manna demand is
  numerically identical to the legacy demand under `NoFinancing`/zero v3
  fields, and `household_baseline` defaults to the legacy Törnqvist base.
- `tests/test_fixed_closure.jl` needed NO updates: baseline still
  `real_gdp ≈ 1`, residuals < 1e-5, uncapped employment, both scale-guard
  substrings intact.
- One solver-surface fix was required: `NonlinearSolve` internals throw
  `ArgumentError` on non-finite trial points instead of returning failure, so
  both solve paths now convert solver-internal failures into the legacy
  `"did not converge"` `ErrorException` (length errors still propagate).
  Bare `LevenbergMarquardt()` was qualified to `NonlinearSolve.` (the root
  module only imports `NonlinearSolve` qualified; cbase2 imports it fully).
- Smoke: BETA(η_s = 0) ≡ ALPHA exactly; BETA(0.5) residual 0.0; DELTA with a
  manna anchor residual ≈ 1e-6; F1/F2/F3 hooks and anchors behave.
- `rerun_results.jl` include paths updated; the full 71-sector rerun is a
  best-effort follow-up (takes minutes) — the tiny/characterization suite is
  the gate and it is green.

**Decision.** ADR-0005 (layout, backport with manna/legacy compatibility, the
intentional all-N fixed change, `diff_kernel.jl` retirement,
`calibration.jl` deferred to Phase 3). Promoted closures stay `implemented`;
contract tests land next and will set `tested`.

**Next.** Phase 2 follow-ups: promoted-closure contract tests (then `tested`
statuses); best-effort `rerun_results.jl` comparison (`ref_gdp ≈
0.9998546537504605`, `sum(λ) ≈ 2.1099`); Phase 3 calibration
(`household_baseline` overwrite), `experiments/run.jl` + manifests,
`scripts/check_repo.jl`. Open gates that remain: income effect, w0 anchor,
Type-I/II identification, `public_budget` pricing, `external_balance` scope,
F3/BETA solver margins, sector-71/high-self-loop stability, clamp mass and
stale 71-sector headlines.
