# registry/closures.toml — labour and financing closures (Phase 0)
#
# Single source of truth for the closure exploration; schema: registry/README.md.
# Status vocabulary: idea | spec | implemented | tested | validated | rejected | superseded.
# Nothing here is validated: the Phase 4 gates of ROADMAP.md have not all passed
# for any closure, and no run manifest is recorded.

schema_version = 1
generated_at = "2026-09-17"
generated_by = "phase-0 agent"

# ═══════════════════════════════════════════════════════════════════════════════
# Labour closures (Foundation I of docs/DOCS_ASSESSMENT.md)
# ═══════════════════════════════════════════════════════════════════════════════

[labor.BF]
id = "BF"
name = "Mobility-endpoint selector (BF reallocation)"
formulation = "eta selects the kept BF endpoints: eta = 0 keeps the baseline allocation L_fixed_i, eta = 1 uses the cost-minimizing demand L_costmin_i; the flexible wage clears the labour market (ADR-0010)"
interpretation = "Static intersectoral allocation selector; eta is NOT a labour-supply elasticity, and eta = 0 is not Baqaee-Farhi 2019's immobile benchmark (one common wage is kept)."
status = "implemented"
symbols = ["MobileLaborCES"]
files = ["src/core/equilibrium.jl", "src/closures/labor/types.jl"]
references = ["docs/labor_closures.md", "docs/DOCS_ASSESSMENT.md", "roadmaps/vertdict.md", "ROADMAP.md"]
tests = ["tests/test_mobile_labor.jl", "tests/test_kernel_regression.jl", "tests/test_external_closure.jl"]
evidence = ["cbase2/process_comments.md", "runs/matrix_5x3-v5-BF-F1/manifest.toml", "runs/matrix_5x3-v5-BF-F2/manifest.toml", "runs/matrix_5x3-v5-BF-F3/manifest.toml"]
dead_ends = ["DE-0001", "DE-0002", "DE-0004"]
adrs = ["ADR-0002", "ADR-0005", "ADR-0010", "ADR-0014", "ADR-0017", "ADR-0019", "ADR-0020"]
open_gates = [
    "eta = 0 keeps one common wage with fixed quantities; BF's immobile case has sector-specific wages (cbase2/review.md §1)",
    "Phase-4 residual gates (ROADMAP.md): all-N clearing residuals (ADR-0019; the omitted-equation rotation is retired), household-expenditure exhaustion, homogeneity, multi-start convergence, Tornqvist consistency",
    "run manifests recorded: runs/matrix_5x3-BF-F1|F2|F3/manifest.toml (executed, 2026-09-17; eta = 0 endpoint)",
    "at eta = 0 all N clearings are now enforced with the F = 0 pin (ADR-0019); the residual canary reading is the documented factor-market gap (4.3e-4..7.9e-3 on the full-71 v5 cells), not an external imbalance",
    "at eta = 0 the reported external position is NOT identified: the F = 0 pin (ADR-0019 D2) substitutes for the labour-supply row (identically zero at eta = 0), so external_position = F + B_gov collapses to the programme booking B_gov = +1.331 percent of GDP, identical in BF-F3, GAMMA-F3 and DELTA-F3, while the resource side S + T_int + M - (I+X) reads +0.5374 percent at BF-F3 (account open by -0.7936 percent, i.e. the pin's labour residual -w*(sum L^cm - sum L) with sum L^cm = 1.0079361 against the frozen bar 1); every pin F = c is an exact root and moves the entry one-for-one (-0.669 to +3.331 percent of GDP for c in -0.02 to +0.02) -- audit in experiments/probes/probe5_bf_f3_external_position.jl, probe5b_bf_f3_followups.jl, probe5c_bf_pin_equivalence.jl; closure options in ADR-0020 (proposed)",
    "eta = 0 with the pin replaced by the labour equation sum L^cm = Lbar closes the account to 4e-16 and returns BF-F1/F2/F3 = ALPHA-F1/F2/F3 exactly, with financing neutrality then holding at eta = 0 too: in the demand-only design the frozen sectoral allocation enters nothing aggregate, so the BF/ALPHA difference is carried by the pin, not by the allocation (ADR-0020 options B and C)",
]
notes = "ADR-0010 retires the interpolated 0 < eta < 1 cases and the ad hoc allocation wedge (never derived as a CES allocative-loss coefficient; cbase2/review.md §1, §2.4). ADR-0019 replaces the N-1/omitted-market reading (ADR-0010): all N clearings are enforced in every regime; the mobile eta = 1 system carries the endogenous net external transfer F with E = (1-tau)wΣL + F, the eta = 0 endpoint pins F = 0, and the booked identity is S + T_int + M - (I+X) = F + B_gov (the ADR-0010 endpoints eta in {0,1} and the retired wedge stand). tests/test_mobile_labor.jl covers the endpoint contract (closure taxonomy, solve, residuals, rejected intermediates); tests/test_kernel_regression.jl pins the eta = 0/1 goldens; tests/test_external_closure.jl covers the all-N contract, the identity gap and financing neutrality. The 2026-09-18 audit of the eta = 0 external account (probes 5/5b/5c) found the position unidentified there and the account open by the pin's labour residual; the eta = 0 closure options and their v6 scope are in ADR-0020 (proposed)."

[labor.ALPHA]
id = "ALPHA"
name = "Full-employment mobile labour"
formulation = "sum_i L_i = Lbar; one economy-wide flexible wage; full cost-minimizing allocation (BF eta = 1)"
interpretation = "Standard full-employment CGE endpoint."
status = "implemented"
symbols = ["MobileLaborCES", "FlexibleWageClosure"]
files = ["src/core/equilibrium.jl", "src/closures/labor/types.jl"]
references = ["ROADMAP.md", "docs/DOCS_ASSESSMENT.md"]
tests = ["tests/test_mobile_labor.jl", "tests/test_external_closure.jl"]
evidence = ["runs/matrix_5x3-v5-ALPHA-F1/manifest.toml", "runs/matrix_5x3-v5-ALPHA-F2/manifest.toml", "runs/matrix_5x3-v5-ALPHA-F3/manifest.toml"]
dead_ends = ["DE-0002"]
adrs = ["ADR-0002", "ADR-0005", "ADR-0010", "ADR-0019", "ADR-0020"]
open_gates = [
    "Phase-4 residual gates (ROADMAP.md)",
    "run manifests recorded: runs/matrix_5x3-ALPHA-F1|F2|F3/manifest.toml (executed, 2026-09-17)",
    "BF at eta = 0 with the pin set to ALPHA's solved F reproduces this cell to 2.8e-17, and replacing the pin by the labour equation sum L^cm = Lbar returns BF = ALPHA exactly (probes 5b/5c, 2026-09-18): in the demand-only design this cell IS the aggregate content of the BF row, so the BF/ALPHA contrast carries no allocation information here (ADR-0020, proposed)",
]
notes = "Nested in BF at eta = 1 by construction; the coincidence is definitional in the code, not a result (cbase2/review.md §1). FlexibleWageClosure is defined in src/closures/labor/types.jl and referenced by src/core/equilibrium.jl. tests/test_mobile_labor.jl covers the mobile closure contract. ADR-0019: the mobile eta = 1 system enforces all N clearings with the endogenous net external transfer F (E = (1-tau)wΣL + F); the booked identity S + T_int + M - (I+X) = F + B_gov holds to ~1e-16 on the v5 ALPHA cells (runs/matrix_5x3-v5-ALPHA-F1|F2|F3/manifest.toml, executed 2026-09-18, commit f0af583)."

[labor.BETA]
id = "BETA"
name = "Elastic total labour supply"
formulation = "sum_i L_i = Lbar * ((w/P)/(w0/P0))^eta_s"
interpretation = "Intended as a labour-leisure / voluntary supply response; the income effect is not implemented."
status = "tested"
symbols = ["ElasticLaborClosure"]
files = ["src/closures/labor/types.jl", "src/closures/labor/labor.jl", "src/core/equilibrium.jl"]
references = ["docs/closures/beta.md", "docs/DOCS_ASSESSMENT.md", "cbase2/documentation.md", "ROADMAP.md"]
tests = ["tests/test_promoted_closures.jl"]
evidence = ["cbase2/process_comments.md", "runs/matrix_5x3-v5-BETA-F1/manifest.toml", "runs/matrix_5x3-v5-BETA-F2/manifest.toml", "runs/matrix_5x3-v5-BETA-F3/manifest.toml"]
dead_ends = ["DE-0004"]
adrs = ["ADR-0002", "ADR-0005", "ADR-0010"]
open_gates = [
    "labour-leisure income effect not implemented (cbase2/review.md §1)",
    "w0 anchor vs baseline wage (cbase2/review.md §3.4)",
    "70-sector BETA continuation margin unverified after promotion (cbase2 timeout, cbase2/process_comments.md 2026-09-16; 3-sector contract tests converge)",
    "matrix cells BETA-F1|F2|F3 executed but numerically IDENTICAL to ALPHA (max|dy| = 6.2e-17, max|dp| = 0, w_raw = 1.000000000000001 in every cell): eta_s is unidentified in the matrix gauge because the equilibrium real wage sits at its baseline value, so the supply curve returns L = Lbar for any eta_s -- the BETA row carries no information beyond ALPHA as designed (2026-09-17, runs/matrix_5x3-BETA-*)",
    "the degeneracy is SHOCK-side, not anchor-side: under the demand-only programme eta_s = 0.5 / 2 / 5 give solutions identical to ~1e-15 (w/P = 0.9999999999999989, L = 1.0), while under a +20 % sector-1 supply shock the real wage moves to 1.00490 and eta_s bites (L = 1.00245 at eta_s = 0.5 vs 1.00983 at eta_s = 2.0) -- BETA is identifiable only with a supply-side scenario (2026-09-17). The documented closure form is the REAL wage (DE-0004) while the implementation uses raw w on the grounds that the CPI numeraire makes P = 1; an explicit CPI deflation is proposed (pending ADR-0014)",
]
notes = "Promoted to src/closures/labor/ in Phase 2 (ADR-0005). cbase2/review.md §1 verdict: partially supported. The power-function supply curve is a defensible reduced form directly answering R2, but the labour-leisure interpretation is not earned by the implementation (no income effect). The v3 verification (solve_beta eta_s-continuation) ran over 25 minutes without completing (cbase2/process_comments.md, 2026-09-16); the v2-stage elasticity identification recovers eta_s by construction and is circular (cbase2/review.md §3.4)."

[labor.GAMMA]
id = "GAMMA"
name = "Fixed real wage (two-sided peg)"
formulation = "w/P = wbar (= 1); employment endogenous and uncapped"
interpretation = "Perfectly elastic labour supply at a fixed real wage; the fixed-wage endpoint of the sweep."
status = "implemented"
symbols = ["FixedWageClosure"]
files = ["src/core/equilibrium.jl", "src/closures/labor/types.jl"]
references = ["ROADMAP.md", "docs/DOCS_ASSESSMENT.md"]
tests = ["tests/test_fixed_closure.jl"]
evidence = ["runs/matrix_5x3-v5-GAMMA-F1/manifest.toml", "runs/matrix_5x3-v5-GAMMA-F2/manifest.toml", "runs/matrix_5x3-v5-GAMMA-F3/manifest.toml"]
dead_ends = ["DE-0003"]
adrs = ["ADR-0002", "ADR-0005", "ADR-0010", "ADR-0014", "ADR-0017"]
open_gates = [
    "the canonical one-sided wage floor with rationed employment is not implemented (that is ZETA; cbase2/review.md §1)",
    "GAMMA-F2/F3 executed (runs/matrix_5x3-GAMMA-F2|F3/manifest.toml); GAMMA-F1 failed the retired heuristic guard in v1 (fixed wage + eta = 1 + F1 has no additive anchor -- kept visible as history) and executes in the v2/v3 generations (runs/matrix_5x3-v2-GAMMA-F1, runs/matrix_5x3-v3-GAMMA-F1/manifest.toml) under the ADR-0017 actual-matrix assessment (column-sum shortcut, else (I − G) rank and positive-solution test)",
    "the ADR-0014 closed-form round-gain trigger is corrected by ADR-0017: the fixed-wage eta = 1 admissibility guard assesses the actual clearing matrix G at the baseline prices (column-sum shortcut, else (I − G) rank and positive-solution test); on the full-71 A-bill calibration the actual max column sum is 0.8314 < 1, so all v2/v3 fixed-wage cells take the shortcut path",
]
notes = "The two-sided uncapped peg is not the canonical Keynesian closure; the doc itself identifies it as BETA's eta_s to infinity limit, so it cannot be an independent paradigm (cbase2/review.md §1). Phase 2 adopts the cbase2 fixed formulation (all N clearings, w = 1 numeraire; ADR-0005), resolving the omitted-N open item. The historical +19.3 pp headline came from a retired unfinanced shock; the financed F2 run is roughly employment-neutral (cbase2/review.md §1; cbase2/process_comments.md, 2026-09-15). tests/test_fixed_closure.jl covers the fixed-closure contract by inspection (uncapped employment, scale guards)."

[labor.DELTA]
id = "DELTA"
name = "IO-type endpoint (Leontief limit)"
formulation = "GAMMA (w/P = 1) + Leontief limit of the CES core (theta, epsilon, sigma -> 0+) with full cost-minimizing allocation (eta = 1)"
interpretation = "Robinson-2006-style fixed-price multiplier corner."
status = "tested"
symbols = ["delta_elasticities", "delta_model", "leontief_multiplier"]
files = ["src/closures/labor/labor.jl", "src/core/equilibrium.jl"]
references = ["docs/closures/delta.md", "ROADMAP.md", "docs/DOCS_ASSESSMENT.md"]
tests = ["tests/test_promoted_closures.jl"]
evidence = ["cbase2/scripts/verify_v3.jl", "runs/matrix_5x3-v5-DELTA-F1/manifest.toml", "runs/matrix_5x3-v5-DELTA-F2/manifest.toml", "runs/matrix_5x3-v5-DELTA-F3/manifest.toml"]
dead_ends = []
adrs = ["ADR-0002", "ADR-0005", "ADR-0010", "ADR-0014", "ADR-0017"]
open_gates = [
    "Type I vs Type II multiplier identification unresolved (cbase2/review.md §1)",
    "exactness rests on p = 1 under demand-only shocks, not on the epsilon limit (cbase2/review.md §3.5)",
    "matrix cells DELTA-F2/F3 executed (resid 8.7e-07 / 4.4e-16; runs/matrix_5x3-DELTA-F2|F3/manifest.toml); DELTA-F1 failed the retired heuristic guard in v1 exactly as pre-registered in the design (kept visible as history) and executes in the v2/v3 generations (runs/matrix_5x3-v2-DELTA-F1, runs/matrix_5x3-v3-DELTA-F1/manifest.toml) under the ADR-0017 actual-matrix assessment (column-sum shortcut, else (I − G) rank and positive-solution test)",
    "the ADR-0014 closed-form round-gain trigger is corrected by ADR-0017: the fixed-wage eta = 1 admissibility guard assesses the actual clearing matrix G at the baseline prices (column-sum shortcut, else (I − G) rank and positive-solution test); on the full-71 A-bill calibration the actual max column sum is 0.8314 < 1, so all v2/v3 fixed-wage cells take the shortcut path",
]
notes = "Promoted to src/closures/labor/labor.jl in Phase 2 (ADR-0005). DELTA is a corner reached as GAMMA plus Leontief technology (docs/DOCS_ASSESSMENT.md; cbase2/documentation.md); it is the endpoint row of the 5x3 matrix but not an independent mechanism (see ADR-0002 amendment 2026-09-17). The analytic counterpart leontief_multiplier includes endogenous consumption, saving and import leaks, i.e. a Type II/SAM-style multiplier rather than the (I-A)^-1 Type I inverse (cbase2/review.md §1). verify_v3.jl asserts relative agreement below 5e-3, not machine zero (cbase2/review.md §2.9)."

[labor.ZETA]
id = "ZETA"
name = "Unemployment complementarity"
formulation = "0 <= Lbar - L  _|_  w/P - omega_bar >= 0 (complementarity / one-sided real-wage floor)"
interpretation = "Demand-constrained employment when initially unused labour exists."
status = "idea"
symbols = []
files = []
references = ["ROADMAP.md", "roadmaps/vertdict.md"]
tests = []
evidence = []
dead_ends = ["DE-0007"]
adrs = ["ADR-0002"]
open_gates = [
    "equations",
    "calibration",
    "implementation",
    "tests",
    "MPEC/smoothing formulation required because plain square solvers cannot express it (cbase2/review.md §4)",
]
notes = "The 2022 Baqaee-Farhi complementarity machinery may be adapted but not copied: do not import its N-sector sticky-labour structure (roadmaps/vertdict.md; docs/DOCS_ASSESSMENT.md)."

# ═══════════════════════════════════════════════════════════════════════════════
# Financing closures (Foundation II of docs/DOCS_ASSESSMENT.md)
# ═══════════════════════════════════════════════════════════════════════════════

[financing.F1]
id = "F1"
name = "Preference reallocation"
formulation = "beta_tilde_i = beta_i d_i / sum_j beta_j d_j; sum_i p_i c_i^h = E_h; budget-neutral composition shift"
interpretation = "Compositional household demand shift; the budget closes by construction within fixed household expenditure; government spending untouched."
status = "tested"
symbols = ["PreferenceReallocation"]
files = ["src/closures/financing/financing.jl"]
references = ["docs/closures/financing.md", "docs/DOCS_ASSESSMENT.md", "cbase2/documentation.md"]
tests = ["tests/test_promoted_closures.jl", "tests/test_f1_tilt.jl"]
evidence = ["cbase2/process_comments.md", "cbase2/scripts/verify_v3.jl"]
dead_ends = ["DE-0001"]
adrs = ["ADR-0002", "ADR-0005", "ADR-0010", "ADR-0014", "ADR-0017"]
open_gates = [
    "F1 cells executed for BF/ALPHA/BETA (runs/matrix_5x3-*-F1/manifest.toml); GAMMA-F1 and DELTA-F1 failed the retired heuristic guard in v1 because F1 has no additive anchor (recorded, not dropped) and execute in the v2/v3 generations under the ADR-0017 actual-matrix assessment (column-sum shortcut, else (I − G) rank and positive-solution test)",
    "the ADR-0014 closed-form round-gain trigger is corrected by ADR-0017: the fixed-wage eta = 1 admissibility guard assesses the actual clearing matrix G at the baseline prices; on the full-71 A-bill calibration the actual max column sum is 0.8314 < 1, so all v2/v3 fixed-wage cells take the shortcut path",
    "v3 saving/import accounting: implemented household demand is (1-s)E with import margins; the ADR-0002 identities hold exactly at s = 0, m = 0 (docs/closures/financing.md)",
]
notes = "A demand-composition shifter, not a financing closure in the CGE taxonomy (cbase2/review.md §1). Must never be described as an autonomous investment multiplier (ROADMAP.md section 4.3; docs/DOCS_ASSESSMENT.md, verdict warning). The explicit tilt is implemented in the canonical pipeline: experiments/run.jl f1_tilt_weights computes d = 1 .+ G0 .* ψ1 ./ c0 (ψ restricted to positive-baseline sectors and renormalised, G0 = sum(g), c0 = data.household_baseline), replacing the retired non-reference 1 .+ ψ tilt (ψ added directly instead of G0 .* ψ1 ./ c0) (f1_shift tilt_g0_over_c0)."

[financing.F2]
id = "F2"
name = "Tax-financed public investment"
formulation = "sum_i p_i g_i = T(p); lump-sum / balanced-budget tax"
interpretation = "Autonomous public investment financed by an explicit tax counterparty (balanced government budget)."
status = "tested"
symbols = ["TaxFinanced"]
files = ["src/closures/financing/financing.jl"]
references = ["docs/closures/financing.md", "docs/DOCS_ASSESSMENT.md", "cbase2/documentation.md"]
tests = ["tests/test_promoted_closures.jl"]
evidence = ["cbase2/process_comments.md"]
dead_ends = []
adrs = ["ADR-0002", "ADR-0005", "ADR-0010", "ADR-0018"]
open_gates = [
    "F2 cells executed for all five labour rows. RESOLVED by ADR-0018 and the matrix_5x3_v4 generation (runs/matrix_5x3-v4-*-F2/manifest.toml, 2026-09-18): the mobile full-employment rows read gdp_rel = 0.0 (income-side real GDP, flat -- the pre-registered 'aggregate ~ 0' signature holds) while consumption_rel = -1.69e-2 is the household-welfare contraction from the tax; the v3 real_gdp_rel = -1.69e-2 column was the welfare index, not GDP",
    "budget helper pricing inconsistency noted in review §2.7",
    "v3 saving/import accounting: implemented household demand is (1-s)E with import margins; the ADR-0002 identities hold exactly at s = 0, m = 0 (docs/closures/financing.md)",
]
notes = "public_budget prices at sum(gov_demand) while taxes are dot(p, gov_demand) (cbase2/review.md §2.7), so government budget reports fail as soon as p != 1. The tax is implemented as an endogenous rate tau = dot(p, gG + g) / (w * sum L) on wage income; with one representative household the burden T(p) is effectively lump-sum."

[financing.F3]
id = "F3"
name = "External / debt-financed programme"
formulation = "sum_i p_i g_i = F; external balance F"
interpretation = "Net new expenditure with an external counterpart; resources arrive via the external balance (imports serve the investment). It is an accounting open-economy closure, not a monetary mechanism."
status = "tested"
symbols = ["ExternalDebt"]
files = ["src/closures/financing/financing.jl"]
references = ["docs/closures/financing.md", "docs/DOCS_ASSESSMENT.md", "cbase2/documentation.md"]
tests = ["tests/test_promoted_closures.jl", "tests/test_external_closure.jl"]
evidence = ["cbase2/process_comments.md", "runs/matrix_5x3-v5-ALPHA-F3/manifest.toml", "runs/matrix_5x3-v5-BETA-F3/manifest.toml", "runs/matrix_5x3-v5-BF-F3/manifest.toml", "runs/matrix_5x3-v5-GAMMA-F3/manifest.toml", "runs/matrix_5x3-v5-DELTA-F3/manifest.toml"]
dead_ends = []
adrs = ["ADR-0002", "ADR-0005", "ADR-0010", "ADR-0019", "ADR-0020"]
open_gates = [
    "F3 cells executed for all five labour rows (runs/matrix_5x3-*-F3/manifest.toml, 2026-09-17); the recorded external position is canary_diff = -8.5e-3 for the mobile eta = 1 rows and ~1e-16 for the fixed-wage rows, while the programme value F = dot(p, g) = 1.33e-2 -- the pre-registered 'external deficit = F' signature needs an agreed definition. RESOLVED by ADR-0019 and the matrix_5x3_v5 generation (15/15 executed 2026-09-18, commit f0af583): the programme financing is booked as B_gov = Σ p·g, the net external position is F + B_gov, and F2/F3 are real-neutral in the mobile regime (identical p, y, w; F_F3 = F_F2 - B_gov)",
    "external_balance returns only the programme's import content (review §2.8) -- RESOLVED as a definition question by ADR-0019 (the diagnostic keeps returning the programme's import content; the external position is the booked F + B_gov)",
    "v3 saving/import accounting: implemented household demand is (1-s)E with import margins; the ADR-0002 identities hold exactly at s = 0, m = 0 (docs/closures/financing.md)",
    "at the BF eta = 0 row the booked entry is the programme booking, not a position: B_gov = +1.331 percent of GDP, identical in BF-F3, GAMMA-F3 and DELTA-F3; GAMMA/DELTA-F3 close their account to ~1e-12 (employment rises to 1.017796, the resources arrive from abroad) while BF-F3 is open by -0.7936 percent of GDP (resource side +0.5374 percent) -- see the labor.BF gates and ADR-0020 (proposed)",
]
notes = "Not a full current-account closure (cbase2/review.md §1). external_balance returns only the programme's import content (cbase2/review.md §2.8), and the recorded F = 0.00147 figure is ambiguous: about half of dot(p, m .* g) at baseline. ADR-0010 adds the asserted external-account canary: at mobile eta = 1 solutions the omitted N-th market residual equals S - (I+X-M) via external_balance_canary, asserted by tests and experiments. ADR-0019 supersedes the omitted-market reading: all N clearings are enforced, the programme financing is booked as B_gov = Σ p·g (B_gov = 0 otherwise), the net position is F + B_gov, and F2/F3 are real-neutral in the mobile regime (v5: B_gov = +1.331% of GDP, F_F3 = -1.432%, F_F2 = -0.101%, identical external position to ~2.6e-17). tests/test_external_closure.jl asserts the neutrality contract. At the BF eta = 0 row the booked B_gov is not a position: the pin fixes F = 0 and the account is open by the pin's labour residual (audit in experiments/probes/probe5*_bf_f3*.jl, 2026-09-18; ADR-0020 proposed)."
